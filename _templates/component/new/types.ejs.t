---
to: <%= componentDir %>/<%= name %>.types.ts
---

export interface Props {
    // add props here
    // don't forget to document them with jsdoc comments
    // ie.
    /** A className to override styling */
    className?: string;
}