export interface User {
    id: number;
    name: string;
    createdAt: string;
    updatedAt: string;
}

export interface Test {
    id: number,
    name: string,
    isActive: boolean,
    createdAt: string,
    updatedAt: string
}